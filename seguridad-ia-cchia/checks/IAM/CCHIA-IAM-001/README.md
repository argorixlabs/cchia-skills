# CCHIA-IAM-001 — Secretos en repositorio

Revisa texto ya recolectado por el motor y reporta únicamente ruta, línea y clase de patrón. Nunca copia el
valor sospechoso a la evidencia. Un `PASS` cubre solo estas heurísticas y debe complementarse con secret
scanning especializado. Ante un positivo real, eliminar el archivo no basta: hay que revocar y rotar.
